# This package is named 'agents' locally. To avoid shadowing the openai-agents
# SDK (also named 'agents'), all SDK imports inside this package must use
# importlib or be done at function scope. See individual agent files.
