"""
Phases module — superseded.

The Planner → Executor → Synthesizer pipeline has been replaced by a single
ReAct loop in loop.py. This file is kept as a stub so any stale imports do
not crash at startup.
"""
